# HOTCOLD Block - victim detector dataset (FLIR ADAS, pedestrian only, h>120px)
# path is resolved relative to victim_detector/ (see utils/general.py check_dataset)
path: ../dataset/FLIR_ADAS
train: images/train
val: images/val

# Classes
nc: 1  # number of classes
names: ['pedestrian']  # class names
